{
  "name": "soolect",
  "private": true,
  "version": "1.0.0",
  "description": "SOOLECT — streetwear brand site (frontend demo, backend & admin coming next).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
